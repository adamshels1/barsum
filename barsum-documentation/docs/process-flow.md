# Barsum — Process Flow & Architecture

## Что такое Barsum

Образовательная платформа для детей в Казахстане. Дети проходят ежедневные учебные челленджи, зарабатывают монеты и обменивают их на реальные награды от родителей. Эксперты-авторы создают и монетизируют программы развития.

**Стек:** Next.js 16 (App Router) + React 19 · NestJS 11 + TypeScript · PostgreSQL + TypeORM · Minio (аудио) · OpenAI (Whisper + GPT) · mobile-first · валюта — тенге (₸) · курс: 1 ₸ = 10 монет

---

## Четыре роли пользователей

| Роль | Описание | Вход в систему |
|------|----------|----------------|
| `child` | Ребёнок, участник челленджей | Логин + пароль (задаёт родитель) |
| `parent` | Родитель, покупает и контролирует | Email + пароль |
| `expert` | Автор-эксперт, создаёт челленджи | Email + пароль, отдельный онбординг |
| `admin` | Администратор Barsum | Email + пароль, внутренний доступ |

---

## Экраны (view) по ролям

### Роль: child
| view | Название | Описание |
|------|----------|----------|
| `home` | Каталог | Лента доступных челленджей с фильтрацией по категориям |
| `session` | Занятие | Ежедневная сессия — чтение, запись, AI-анализ |
| `shop` | Магазин | Обмен монет на награды + трекер мечты |

### Роль: parent
| view | Название | Описание |
|------|----------|----------|
| `home` | Каталог | Просмотр и покупка челленджей для ребёнка |
| `cabinet` | Кабинет | Дашборд детей, купленные челленджи, очередь проверки |
| `rewards` | Награды | Управление наградами, одобрение запросов |

### Роль: expert
| view | Название | Описание |
|------|----------|----------|
| `ex_home` | Главная эксперта | Метрики, очередь ревью, сообщения |
| `ex_books` | Каталог | Все опубликованные челленджи |
| `create` | Создание | Wizard (4 шага) создания нового челленджа |

### Роль: admin
| view | Название | Описание |
|------|----------|----------|
| `payments` | Оплаты | Очередь чеков Kaspi Pay — подтверждение/отклонение |
| `experts` | Эксперты | Модерация заявок экспертов (new → approved/rejected) |
| `challenges` | Челленджи | Модерация опубликованных челленджей |

---

## Категории контента

> **Скоуп v1:** реализуется только категория `reading`. Остальные категории — следующие версии.

| ID | Название | Цвет | Статус |
|----|----------|------|--------|
| `reading` | Чтение | `#6347E0` (фиолетовый) | ✅ **v1** |
| `logic` | Логика | `#D14E84` (розовый) | ⏳ позже |
| `finance` | Финансы | `#C8861F` (золотой) | ⏳ позже |
| `english` | Английский | `#3470DB` (синий) | ⏳ позже |
| `sport` | Спорт | `#12895F` (зелёный) | ⏳ позже |
| `creativity` | Творчество | `#D2652F` (оранжевый) | ⏳ позже |

---

## Скоуп v1

> Реализуется в первой версии. Всё остальное — следующие итерации.

| Блок | v1 | Позже |
|------|----|-------|
| Категории | Только `reading` (Чтение) | logic, finance, english, sport, creativity |
| Челленджи | Только читательские (книги) | Все остальные типы |
| Ежедневные задачи | Прочитать → Голосовой пересказ → Whisper транскрибация → AI анализ → 3 вопроса AI | Другие форматы заданий |
| Авторизация | Email + пароль (родитель/эксперт), логин + пароль (ребёнок) | OAuth, соцсети |
| Экран ребёнка | Баланс монет + активные челленджи | Полный профиль, ачивки |
| Магазин наград | Базовый список + кнопка «Отправить в мечту» | Расширенный каталог, авто-накопление |
| Dream savings | Ручная кнопка «Отправить в мечту» | Авто 10% (A/B эксперимент) |
| Сертификаты | — | Цифровые сертификаты |
| Эксперт | ✅ Кабинет эксперта + wizard создания (4 шага) | — |
| AI-модерация | ✅ Автоматический зачёт > 80%, ручная проверка < 80% | — |
| Оплата | ✅ Kaspi Pay — ручное подтверждение чека администратором | Прямая API-интеграция Kaspi Pay, TipTop |
| Админ-панель | ✅ Подтверждение чеков, модерация экспертов и челленджей | — |

---

## Навигационная карта

```mermaid
flowchart TD
    START([Открытие приложения]) --> ROLE{Выбор роли}

    ROLE -->|Email + пароль| PARENT_CAB
    ROLE -->|Логин + пароль\nот родителя| CHILD_HOME
    ROLE -->|Expert логин| EX_HOME

    subgraph CHILD [👦 Ребёнок]
        CHILD_HOME["home\n💰 Баланс монет вверху\nКаталог челленджей"]
        CHILD_HOME --> SESSION[session\nЕжедневное занятие]
        CHILD_HOME --> SHOP[shop\nМагазин наград]
        SESSION --> CHILD_HOME
    end

    subgraph PARENT [👩 Родитель — email-логин]
        PARENT_CAB[cabinet\nДети + добавление аккаунтов] --> PARENT_HOME[home\nКаталог для покупки]
        PARENT_CAB --> REWARDS[rewards\nНаграды]
        PARENT_HOME --> PARENT_CAB
    end

    subgraph EXPERT [🎓 Эксперт]
        EX_HOME[ex_home\nГлавная эксперта]
        EX_HOME --> EX_BOOKS[ex_books\nКаталог]
        EX_HOME --> CREATE[create\nСоздание челленджа]
        CREATE --> EX_HOME
    end
```

---

## Флоу 1: Регистрация / Вход

```mermaid
flowchart TD
    OPEN([Открытие приложения]) --> ROLE{Кто входит?}

    ROLE -->|Родитель| AUTH_P[Экран авторизации родителя]
    ROLE -->|Ребёнок| AUTH_C[Экран входа ребёнка]
    ROLE -->|Эксперт| AUTH_E[Экран входа эксперта]

    subgraph PARENT_AUTH [Авторизация родителя]
        AUTH_P --> REG[Регистрация\nEmail + пароль\nEmail = логин]
        AUTH_P --> LOGIN[Вход\nEmail + пароль]
    end

    REG --> ONBO[Онбординг\nДобавить первого ребёнка]
    LOGIN --> CAB[cabinet\nКабинет родителя]
    ONBO --> CAB

    subgraph CHILD_AUTH [Вход ребёнка]
        AUTH_C --> CHILD_LOGIN[Логин + пароль\nзаданные родителем\nне email]
    end
    CHILD_LOGIN --> CHILD_HOME[home\nrole: child\nБаланс монет виден]

    AUTH_E --> EX[ex_home\nrole: expert]

    style REG fill:#6347E0,color:#fff
    style LOGIN fill:#6347E0,color:#fff
    style CHILD_LOGIN fill:#12895F,color:#fff
    style AUTH_E fill:#3470DB,color:#fff
    style CHILD_HOME fill:#E2F3EC,stroke:#12895F
```

---

## Флоу 1б: Родитель — добавление ребёнка и передача доступа

```mermaid
flowchart TD
    CAB([cabinet — кабинет родителя]) --> ADD[Кнопка + Добавить ребёнка]

    ADD --> FORM["Форма добавления\n──────────────────────\n• Имя ребёнка\n• Возраст\n• Логин (придумать — не email)\n• Пароль (придумать)"]

    FORM --> SAVE[Сохранить]

    SAVE --> CREATED["Аккаунт ребёнка создан\n──────────────────────\nОтображается карточка:\nИмя · Логин · Пароль ●●●● 👁"]

    CREATED --> SHARE["Блок быстрой передачи доступа\n──────────────────────────────\n[ Логин: ayla_2024   📋 ]\n[ Пароль: qwerty123  📋 ]\n[ Скопировать всё    📋 ]\nили\n[ Поделиться ↗ ] — share sheet"]

    SHARE --> CHILD_LOGIN([Ребёнок входит сам\nна своём устройстве])

    style FORM fill:#EEEAFB,stroke:#6347E0
    style CREATED fill:#E6EEFC,stroke:#3470DB
    style SHARE fill:#E2F3EC,stroke:#12895F
    style CHILD_LOGIN fill:#E2F3EC,stroke:#12895F
```

### Правила аккаунта ребёнка

| Поле | Требования |
|------|-----------|
| Логин | Произвольный (не email), уникальный в системе, напр. `ayla_2024` |
| Пароль | Задаёт родитель, ребёнок может сменить позже |
| Привязка | Ребёнок привязан к аккаунту родителя |
| Копирование | Логин и пароль копируются одной кнопкой — для передачи в мессенджер |

---

## Флоу 2: Ребёнок — главный экран и ежедневное занятие

```mermaid
flowchart TD
    LOGIN([Ребёнок вошёл по логин/пароль]) --> HOME

    HOME["home — Главный экран ребёнка\n──────────────────────────────\n💰 Баланс: 8 240 монет  ← всегда виден вверху\n─────────────────\nКаталог активных и доступных\nчелленджей по категориям"]

    HOME -->|Выбрать и запустить| READ

    subgraph SESSION [view: session]
        READ["📖 ФАЗА 1 — ЧТЕНИЕ\nsessionPhase: read\n─────────────────\n• Таймер идёт посекундно\n• Ребёнок читает 10–15 стр.\n• Кнопка микрофона для записи"]
        READ -->|Начать запись пересказа| REC
        REC["🎙️ ФАЗА 2 — ЗАПИСЬ\nsessionPhase: recording\n─────────────────\n• Ребёнок пересказывает вслух\n• Таймер записи\n• Кнопка Стоп"]
        REC -->|Остановить| TRANSCRIBE
        TRANSCRIBE["✍️ ФАЗА 3 — ТРАНСКРИБАЦИЯ\nsessionPhase: transcribing\n─────────────────\n• AI (Whisper) переводит аудио → текст\n• Прогресс-бар\n• Текст пересказа показывается ребёнку"]
        TRANSCRIBE -->|текст готов| ANALYZE
        ANALYZE["🤖 ФАЗА 4 — АНАЛИЗ\nsessionPhase: analyzing\n─────────────────\n• AI проверяет текст пересказа\n• Оценивает полноту и понимание\n• AI задаёт 3 вопроса по тексту\n• Ребёнок отвечает"]
        ANALYZE -->|оценка готова| DONE
        DONE["🏆 ФАЗА 5 — РЕЗУЛЬТАТ\nsessionPhase: done\n─────────────────\n• Монеты начислены\n• Streak увеличен 🔥\n• Текст пересказа сохранён\n• Кнопка Домой"]
    end

    DONE -->|exitSession| HOME

    style READ fill:#EEEAFB,stroke:#6347E0
    style REC fill:#FBE9F1,stroke:#D14E84
    style TRANSCRIBE fill:#FAF0DA,stroke:#C8861F
    style ANALYZE fill:#E6EEFC,stroke:#3470DB
    style DONE fill:#E2F3EC,stroke:#12895F
```

### Детальный флоу AI-обработки (транскрибация + анализ)

```mermaid
flowchart TD
    AUDIO([Аудио пересказа ребёнка]) --> WHISPER

    WHISPER["🎙️ Транскрибация\nOpenAI Whisper или аналог\n─────────────────\nАудио → текст на русском\nВремя: ~5–15 сек"]

    WHISPER --> QUALITY{Качество\nтранскрипции?}

    QUALITY -->|уверенность > 80%| GPT
    QUALITY -->|уверенность < 80%| LOW["⚠️ Низкое качество записи\n─────────────────\nПопросить перезаписать\nили отправить родителю"]

    GPT["🤖 AI-анализ текста\nGPT или аналог\n─────────────────\nПроверяет:\n• Упомянуты ли ключевые события\n• Понял ли ребёнок сюжет\n• Задаёт 3 уточняющих вопроса"]

    GPT --> SCORE{Оценка\nпересказа}

    SCORE -->|хороший пересказ| COINS["✅ Зачтено\nМонеты начислены\nStreak +1\nТекст сохранён"]
    SCORE -->|слабый пересказ| HINT["💡 Подсказка\nAI объясняет что упущено\nПредлагает попробовать ещё раз\nили засчитать частично"]

    LOW -->|отправить на ревью| PARENT_Q[reviewQueue родителя]
    HINT -->|родитель решает| PARENT_Q

    style WHISPER fill:#FAF0DA,stroke:#C8861F
    style GPT fill:#E6EEFC,stroke:#3470DB
    style COINS fill:#E2F3EC,stroke:#12895F
    style HINT fill:#EEEAFB,stroke:#6347E0
    style LOW fill:#FBE9F1,stroke:#D14E84
    style PARENT_Q fill:#FBE9F1,stroke:#D14E84
```

### Ежедневные задачи — v1 (только Чтение)

| Категория | Задача 1 | Задача 2 | Задача 3 | Статус |
|-----------|----------|----------|----------|--------|
| **reading** | Прочитать главу (10–15 стр.) | Ответить на 3 вопроса AI | Голосовой пересказ | ✅ v1 |
| finance | Изучить тему дня | Практическая задача | Записать вывод в дневник | ⏳ позже |
| logic | Решить 3 головоломки | Объяснить ход решения | Пройти мини-тест | ⏳ позже |
| english | Выучить 5 слов | Прочитать текст | Произнести вслух (запись) | ⏳ позже |
| sport | Упражнения дня | Снять короткое видео | Отметить самочувствие | ⏳ позже |
| creativity | Творческое задание | Сфотографировать работу | Описать идею | ⏳ позже |

---

## Флоу 3: Родитель — покупка челленджа

```mermaid
flowchart TD
    LOGIN([Родитель логинится]) --> CAT[home\nКаталог челленджей\nКнопка Купить вместо Начать]
    CAT -->|Выбрать челлендж| DETAIL[Детальная карточка\nНазвание · Автор · Возраст\nДлительность · Участники · Результат]
    DETAIL -->|Купить для ребёнка| MODAL

    subgraph MODAL [Модал покупки]
        CHPRICE["📘 Челлендж\nЦена фиксирована — задаёт автор\nнапр. 5 000 ₸"]
        SLIDER["🎮 Игровые монеты\nСлайдер: 0 – 50 000 монет\nшаг 1 000 монет = 100 ₸\nКурс: 1 ₸ = 10 монет"]
        FORMULA["📊 Куда идут деньги\n──────────────────────────────\nplatform = challengePrice × 15%\nexpert   = challengePrice − platform\ncoinsTg  = coinsAmount ÷ coinRate\ntotal    = challengePrice + coinsTg\n──────────────────────────────\nКомиссия ВНУТРИ цены, не сверху"]
        CHPRICE --> FORMULA
        SLIDER --> FORMULA
    end

    FORMULA -->|Купить челлендж| PAY[Оплата total ₸]

    PAY --> RESULT

    subgraph RESULT [После оплаты]
        P_VIEW["👩 У родителя cabinet\n• Челлендж в списке купленных\n• Прогресс ребёнка виден"]
        C_VIEW["👦 У ребёнка home\n• Челлендж появился в активных\n• coinsAmount зачислены на parentBalance\n  и зарезервированы под челлендж\n• Можно начать занятия"]
    end

    style CHPRICE fill:#EEEAFB,stroke:#6347E0
    style SLIDER fill:#E2F3EC,stroke:#12895F
    style FORMULA fill:#FAF0DA,stroke:#C8861F
    style P_VIEW fill:#EEEAFB,stroke:#6347E0
    style C_VIEW fill:#E2F3EC,stroke:#12895F
```

### Формулы расчёта (из спеки)

```js
coinsTg   = coinsAmount / coinRate          // 10000 / 10 = 1 000 ₸
platform  = round(challengePrice × 0.15)   // 5000 × 0.15 = 750 ₸
expert    = challengePrice - platform       // 5000 − 750 = 4 250 ₸
total     = challengePrice + coinsTg        // 5000 + 1000 = 6 000 ₸
```

> **Важно:** комиссия 15% зашита **внутри** цены челленджа — не накручивается сверху.
> Итого = цена челленджа + цена монет, и ничего больше.

### Пример: челлендж «Волшебный мир книг»

| Строка | Сумма |
|--------|-------|
| Челлендж (challengePrice) | 5 000 ₸ |
| → Эксперт (85%) | 4 250 ₸ |
| → Barsum (15%) | 750 ₸ |
| Игровые монеты · 10 000 шт. (coinsTg) | 1 000 ₸ |
| **Итого к оплате** | **6 000 ₸** |

### Payload при нажатии «Купить челлендж»

```js
{
  challengeId,
  challengePrice,  // 5000
  coinsAmount,     // 10000
  coinsPrice,      // 1000
  total,           // 6000
  currency: "KZT",
  authorId,
  commissionPct    // 0.15
}
```

### Входные данные с бэка (конфигурируемые)

| Параметр | Описание | Пример |
|----------|----------|--------|
| `challengePrice` | Цена челленджа в ₸, задаёт автор | 5 000 |
| `coinRate` | Сколько монет за 1 ₸ | 10 |
| `commissionPct` | Комиссия платформы | 0.15 |
| `coinsMin/Max/Step/Default` | Параметры слайдера | 0 / 50 000 / 1 000 / 10 000 |
| `author` | Имя, рейтинг, id, ссылка на профиль | — |

---

## Флоу 4: Магазин наград (Ребёнок)

```mermaid
flowchart TD
    SHOP([view: shop]) --> TABS

    subgraph TABS [Вкладки магазина]
        TAB1[Награды] 
        TAB2[Мечта]
        TAB3[Сертификаты]
        TAB4[Вывод]
    end

    TAB1 --> REWARDS_LIST["Список наград\n─────────────────\nМороженое — 200 монет\n1 час прогулки — 300\nЧас на приставке — 500\nПицца — 1 500\nПоход в кино — 3 000\nАквапарк — 5 000"]
    TAB2 --> DREAM["Трекер мечты\n─────────────────\nЦель: накопить N монет\nПрогресс-бар: saved / target\nПример: Велосипед 12 000"]

    REWARDS_LIST --> CHECK{coins >= cost?}
    CHECK -->|Да| BUY[coins -= cost\nЗапрос родителю\nToast: куплено!]
    CHECK -->|Нет| BLOCK[Пока не хватает монет\nПочитай ещё!]

    DREAM --> DREAM_STATUS["Статус мечты\nnone → pending → approved"]
    DREAM_STATUS -->|Родитель одобрил| SAVING[Монеты идут на мечту]

    style CHECK fill:#FBE8DD,stroke:#D2652F
    style BUY fill:#E2F3EC,stroke:#12895F
    style BLOCK fill:#FBE9F1,stroke:#D14E84
    style SAVING fill:#E2F3EC,stroke:#12895F
```

### Очередь наград у Родителя

```mermaid
flowchart LR
    REQ([Запрос от ребёнка]) --> QUEUE[view: rewards\nОчередь запросов]
    QUEUE --> APPROVE{Родитель}
    APPROVE -->|Одобрить| DONE[Награда выдана]
    APPROVE -->|Отклонить| REJECT[Запрос отклонён]

    style APPROVE fill:#EEEAFB,stroke:#6347E0
    style DONE fill:#E2F3EC,stroke:#12895F
    style REJECT fill:#FBE9F1,stroke:#D14E84
```

---

## Флоу 5: Эксперт — создание челленджа (Wizard)

```mermaid
flowchart TD
    START([view: create]) --> S1

    subgraph WIZARD [createStep: 1 → 2 → 3 → 4]
        S1["ШАГ 1 — Описание\n──────────────────\n• Название челленджа\n• Выбор категории (6 чипов)\n• Возрастная группа\n• Привязка книги"]
        S2["ШАГ 2 — Параметры\n──────────────────\n• Длительность (дней) ±\n• Дневная цель (страниц/задач)"]
        S3["ШАГ 3 — Цена\n──────────────────\n• Цена для родителя ±500 ₸\n• Расчёт заработка автора\n• Предварительный просмотр"]
        S4["ШАГ 4 — Публикация\n──────────────────\n• Preview финальной карточки\n• Кнопка Опубликовать"]
        S1 --> S2 --> S3 --> S4
    end

    S4 -->|Опубликовать| MODERATION[Модерация Barsum]
    MODERATION -->|Одобрено| CATALOG[Челлендж в каталоге]
    MODERATION -->|Отклонено| EDIT[Вернуть на доработку]
    EDIT --> S1

    style S1 fill:#EEEAFB,stroke:#6347E0
    style S2 fill:#E6EEFC,stroke:#3470DB
    style S3 fill:#FAF0DA,stroke:#C8861F
    style S4 fill:#E2F3EC,stroke:#12895F
    style MODERATION fill:#FBE9F1,stroke:#D14E84
```

---

## Флоу 6: Эксперт — онбординг

```mermaid
stateDiagram-v2
    [*] --> new : Первый вход эксперта

    new : 📝 Заявка (new)\nСпециализация + Опыт работы\nКнопка Подать заявку

    new --> review : Подать заявку

    review : ⏳ На рассмотрении (review)\nОжидание модерации Barsum

    review --> approved : Barsum одобрил
    review --> new : Barsum отклонил

    approved : ✅ Одобрен (approved)\nДоступен полный кабинет\nМетрики · Очередь · Создание

    approved --> [*]
```

---

## Флоу 7: AI-модерация сессий

```mermaid
flowchart TD
    REC([Ребёнок отправил запись]) --> AI[AI анализирует голосовой пересказ]

    AI --> CONF{Уверенность AI}

    CONF -->|> 80%| AUTO[Автоматически засчитан\nМонеты начислены\nStreak +1]
    CONF -->|< 80%| QUEUE[Попадает в reviewQueue\nродителя]

    QUEUE --> REASON["Причина:\n• AI не распознал часть записи\n• Низкая уверенность — 62%"]
    REASON --> PARENT{Родитель проверяет\nвручную}

    PARENT -->|Засчитать| COUNTED[День засчитан\nМонеты начислены]
    PARENT -->|Отклонить| REJECTED[День не засчитан]

    style AUTO fill:#E2F3EC,stroke:#12895F
    style QUEUE fill:#FAF0DA,stroke:#C8861F
    style COUNTED fill:#E2F3EC,stroke:#12895F
    style REJECTED fill:#FBE9F1,stroke:#D14E84
    style CONF fill:#EEEAFB,stroke:#6347E0
```

---

## Экономика монет (закрытая валюта)

Монета — **внутренняя закрытая валюта**: не выводится в реальные деньги, не сгорает.
Один и тот же запас монет крутится по кругу между балансом родителя и балансом ребёнка.
Реальные деньги входят в систему только один раз — при покупке монет.

**Курс: 1 ₸ = 10 монет** (не 1:1).

### Два баланса

| Баланс | Описание |
|--------|----------|
| `parentBalance` | Кошелёк родителя в монетах. Пополняется при покупке и при возврате монет от ребёнка. |
| `childBalance` | Кошелёк ребёнка в монетах. Пополняется за выполненные челленджи, тратится в магазине наград. |

### Цикл движения монет

```mermaid
flowchart LR
    MONEY["💳 Реальные деньги\n₸ → монеты"] -->|покупка| PB

    PB["👩 parentBalance\nзапас монет"]
    PB -->|назначение награды\nза челлендж| RES

    RES["🔒 Резерв челленджа\nзарезервировано"]
    RES -->|AI подтвердил выполнение| CB

    CB["👦 childBalance\nначислено"]
    CB -->|обмен в магазине наград| PB

    style MONEY fill:#FAF0DA,stroke:#C8861F
    style PB fill:#EEEAFB,stroke:#6347E0
    style RES fill:#E6EEFC,stroke:#3470DB
    style CB fill:#E2F3EC,stroke:#12895F
```

### Таблица транзакций (леджер)

| Событие | Откуда | Куда | Статус |
|---------|--------|------|--------|
| Покупка монет (₸ → монеты) | эмиссия | `parentBalance` | confirmed |
| Запись на челлендж | `parentBalance` | резерв челленджа | reserved |
| Челлендж выполнен, AI подтвердил | резерв челленджа | `childBalance` | confirmed |
| Ребёнок запросил награду | `childBalance` | `rewardPending` | pending |
| Родитель нажал «Награда выдана» | `rewardPending` | `parentBalance` | confirmed |
| Родитель отклонил награду | `rewardPending` | `childBalance` | returned |

> Ключевое: монеты возвращаются родителю **только после нажатия «Награда выдана»** — не сразу при запросе.
> Это замкнутый круг: родитель раздаёт вернувшиеся монеты в следующих челленджах.

### Правила

- Курс ₸ → монеты применяется только при покупке. Возврат в монетах 1:1, без обратной конвертации в ₸.
- Монеты резервируются **сразу** при записи на челлендж. Доступный `parentBalance` уменьшается немедленно.
- Нельзя потратить больше, чем есть на `childBalance`; нельзя записать на челлендж если `parentBalance` < стоимость.
- Автоматическое 10% отчисление в dream-savings **убрано**. Ребёнок вручную нажимает «Отправить в мечту».
- Идемпотентность обязательна — повторный клик или ретрай не должны задвоить операцию.
- История транзакций (леджер) ведётся по всем балансам.

---

## Флоу 8: Оплата через Kaspi Pay (ручное подтверждение)

```mermaid
flowchart TD
    BUY([Родитель нажал «Купить челлендж»]) --> DETAILS

    DETAILS["Экран оплаты\n──────────────────────\nСумма: 6 000 ₸\nKaspi Pay: +7 XXX XXX XX XX\nили QR-код\nКнопка: Я оплатил"]

    DETAILS -->|Я оплатил| UPLOAD["Загрузить скриншот чека\nKaspi Pay"]

    UPLOAD --> SENT["Чек отправлен\n──────────────────────\nСтатус: ожидает подтверждения\nУведомление администратору"]

    SENT --> ADMIN{Администратор\nпроверяет чек}

    ADMIN -->|Подтвердить| ACTIVE["Челлендж активирован\n──────────────────────\nМонеты зачислены на parentBalance\n10 000 монет зарезервированы\nУведомление родителю"]

    ADMIN -->|Отклонить| REJECT["Оплата отклонена\n──────────────────────\nПричина отклонения\nУведомление родителю"]

    style DETAILS fill:#EEEAFB,stroke:#6347E0
    style SENT fill:#FAF0DA,stroke:#C8861F
    style ACTIVE fill:#E2F3EC,stroke:#12895F
    style REJECT fill:#FBE9F1,stroke:#D14E84
    style ADMIN fill:#E6EEFC,stroke:#3470DB
```

### Статусы платежа

| Статус | Описание |
|--------|----------|
| `pending` | Чек загружен, ожидает проверки |
| `confirmed` | Администратор подтвердил — челлендж активирован |
| `rejected` | Администратор отклонил — родитель получил уведомление |

> В v2 планируется прямая API-интеграция Kaspi Pay и TipTop для автоматического подтверждения.

---

## Структура данных (ключевые объекты)

### Челлендж
```js
{
  title: '30 дней чтения',
  author: 'Даулет Мукаев',
  cat: 'reading',         // категория
  age: '7–12 лет',
  days: '30 дней',
  books: '1 книга',
  members: '1250',        // кол-во участников
  price: '19 900 ₸',      // цена для родителя
  earn: '14 950 ₸',       // заработок автора
  hit: true               // флаг "хит"
}
```

### Ребёнок
```js
{
  id: Date.now(),
  name: 'Айла',
  age: '9 лет',
  cat: 'reading',         // текущая категория
  books: '3',
  pages: '126',
  streak: '14',           // 🔥 серия дней
  earned: '8 240 ₸',
  progress: '64%',
  activeCh: '2'           // активных челленджей
}
```

### Награда
```js
{
  id: 1,
  name: 'Мороженое',
  cost: 200,              // в монетах
  cat: 'creativity',
  type: 'Вкусняшка'       // Вкусняшка | Время | Впечатление
}
```

---

## Начальное состояние приложения

```js
state = {
  role: 'child',           // child | parent | expert | admin
  view: 'home',
  activeCat: 'reading',
  coins: 8240,             // childBalance
  sessionPhase: 'read',    // read | recording | transcribing | analyzing | done
  recording: false,
  seconds: 0,
  analyze: 0,              // 0–100 прогресс AI-анализа
  authed: false,
  expertStatus: 'new',     // new | review | approved
  buyOpen: false,
  detailIdx: null,
  childrenList: [],
  rewardsList: [...],
  dream: { name:'', target:0, saved:0 },  // без авто-отчисления
  reviewQueue: [],         // очередь ручной AI-проверки
  paymentQueue: [],        // очередь чеков Kaspi Pay (admin)
}
```
